# 分析

方法一：
	将头尾初始化为-1
	最后一个元素出队后将头尾设为-1
	判断是否为空只需检测头是否为-1
	判断是否为满判断尾是否追上头
	入队时只需将尾向后移动1
	出队时只需将头向后移动1

方法二：
	将头尾初始化为0，1
	入队时数据入队，尾向后移1
	出队时头向后移动1
	如果尾在头的前一个，则说明为空
	如果尾等于头，则说明队满
	这种结构牺牲队列里的一个空间

方法三：
	增加一个变量记录当前入队数据个数
	与空间相比较

方法四：

	每次入队或出队后计算长度



#程序见文件夹内

	


# 程序见本文件夹