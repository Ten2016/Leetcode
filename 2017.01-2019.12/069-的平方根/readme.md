# 分析

方法一：
	针对python3.x
	使用math模块的sqrt()方法和floor()方法
	
	针对python2.7
	由于使用floor()会有小数点
	额外使用字符串的rstrip()方法去掉小数点和后面的0

方法二：
	通用所有语言
	正规方法
	牛顿法
	此方法为泰勒一阶展开的迭代式，不解释
	应用到平方根
		f'(Xn)=dy/dx=(f(Xn)-0)/(Xn-X(n+1))
		即：X(n+1)=Xn-f(Xn)/f'(Xn)
	迭代式为:X(n+1)=(Xn+p/Xn)/2，p为需要开方的数
	初始时Xn可随意取，通常取1
	